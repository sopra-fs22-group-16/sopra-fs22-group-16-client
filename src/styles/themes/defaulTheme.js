import {createTheme} from "@mui/material";

const defaultTheme = createTheme({
    palette: {
        primary: {
            main: '#FBC12E',
        },
        secondary: {
            main: '#292420',
        }
    },
});

export default defaultTheme;